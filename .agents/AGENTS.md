# Workspace Rules

## Styling & Layout
- **No Hover Scaling/Shadow changes on guest/auth containers**: Never add hover effects (scaling, shifting shadows, border-color shifts, or background updates) to the main authorization card containers (`.login-card-container`) or the guest profile dropdown container (`.guest-dropdown-menu`). Keep these elements static on hover.
